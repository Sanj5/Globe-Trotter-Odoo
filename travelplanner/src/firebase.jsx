// Firebase is disabled - using backend JWT authentication instead
// If you need Firebase later, configure the environment variables in .env

// Dummy exports to prevent import errors
export const auth = null;
export const provider = null;
export const db = null;
